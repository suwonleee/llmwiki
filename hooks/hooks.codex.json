{
  "description": "llmwiki read-injection for Codex. Same two hooks as hooks.json (the Claude surface), plus the one field only Codex has: additionalContextLimit 0 disables its 10,000-byte stdout spill, which would otherwise replace a large cold start with a head/tail preview. Kept in a separate file because Claude Code's --strict validation rejects fields it does not know.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/sessionstart-inject.sh\"",
            "statusMessage": "llmwiki cold-start",
            "additionalContextLimit": 0
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/userpromptsubmit-inject.sh\"",
            "timeout": 10,
            "statusMessage": "llmwiki turn-context",
            "additionalContextLimit": 0
          }
        ]
      }
    ]
  }
}
