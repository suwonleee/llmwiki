{
  "name": "llmwiki",
  "version": "0.11.2",
  "description": "local-first compounding wiki engine (Bun/TypeScript)",
  "type": "module",
  "private": true,
  "bin": {
    "llmwiki": "src/cli.ts"
  },
  "scripts": {
    "cli": "bun run src/cli.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check:quick": "bun run src/dev/check.ts --quick",
    "check": "bun run src/dev/check.ts"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
