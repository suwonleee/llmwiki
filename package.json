{
  "name": "llmwiki",
  "version": "0.1.1",
  "description": "local-first compounding wiki engine (Bun/TypeScript)",
  "type": "module",
  "private": true,
  "bin": {
    "llmwiki": "src/cli.ts"
  },
  "scripts": {
    "cli": "bun run src/cli.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
