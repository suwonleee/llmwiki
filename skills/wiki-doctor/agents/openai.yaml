# generated by src/plugin/build-assets.ts — edit skill/*.md, then re-run
# Invocation gate: this skill is human-invoked by design (see SKILL.md). Dropping the line
# below lets Codex run a close-out or a deep pass on its own, mid-task.
interface:
  display_name: "$wiki-doctor"
  short_description: "Diagnose and repair the active llmwiki installation and this repository's wiki — owned wiring f…"

policy:
  allow_implicit_invocation: false
