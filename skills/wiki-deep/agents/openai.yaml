# generated by src/plugin/build-assets.ts — edit skill/*.md, then re-run
# Invocation gate: these skills are human-invoked by design (see SKILL.md). Dropping
# allow_implicit_invocation lets Codex run a close-out or a deep pass on its own, mid-task.
interface:
  display_name: "$wiki-deep"
  short_description: "Periodic deep pass (day end · weekly · when a close-out report recommends it) — drain the trans…"

policy:
  allow_implicit_invocation: false
